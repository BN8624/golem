# STATUS

- 아이디어: Extend a deterministic, HERO-ONLY tactical-grid combat kernel. HARD INVARIANTS YOU MUST NOT CHANGE OR REINTERPRET: there is NO enemy turn, NO enemy AI, NO enemy movement, NO enemy mana — enemies are passive targets that never act on their own. The ONLY actor is the hero, and the ONLY actions are the hero's, taken from a fixed ordered action list: move {dir:[dx,dy]}, attack {target:id} (melee), ranged_attack {target:id}. 'turn' is simply incremented by 1 per hero action attempt; there is no global round with multiple entities. Existing five cards (all about the hero acting on passive enemies): (1) hero.mana is a Mana Shield that absorbs the damage the hero RECEIVES from a melee exchange before it hits hero.hp; if hero.mana crosses from >0 to <=0 in one melee attack the shield RUPTURES and an ANOMALY discharges hero.anomaly_dmg to every orthogonally-adjacent living enemy (doubled if the hero stands on Conductive terrain). Only the hero has mana; ANOMALY is triggered ONLY by a melee rupture, never by ranged or terrain alone. (2) ranged_attack deals one-way damage to an enemy at Manhattan distance 2..3 (hero takes nothing). (3) static terrain map of 'x,y'->'Wall'|'Conductive'; Wall blocks the hero's move (not ranged). (4) enemy unit_type Hardened (melee to it -1), Glass (all incoming damage to it x2), Resonant (hero takes 1 self-damage each time this enemy is hit by an anomaly). (5) route: chain of battles; when the current battle's enemies are all dead and the hero lives, load the next battle (hero hp/mana/atk persist, pos resets to 0,0); final battle cleared = VICTORY, hero dead = DEFEAT, actions exhausted = FINISHED. Scenario via --scenario N: initial state {hero:{hp,atk,pos,mana,anomaly_dmg}, enemies:[{id,hp,atk,pos,unit_type?}], terrain?, route?} + action list. Deterministic, no RNG. CLI prints status/turn/hero_hp/hero_pos/enemies (each enemy exactly {id,hp,pos}); this output is FIXED and must not change. NEW CARD 'status effects (상태이상)': the hero's attacks can inflict a lingering effect on an ENEMY that resolves over the hero's subsequent action steps (since only the hero takes turns). For example a Poison/Bleed that deals fixed damage to the afflicted enemy each time the hero takes an action, for a fixed duration. Design the exact deterministic rules yourself: which effect(s), which hero action inflicts it and under what condition, how it is stored as data on the enemy entity, EXACTLY when it ticks and decays relative to each hero action and the post-action win/loss check, stacking vs refresh, and interaction with melee/ranged/mana/anomaly/terrain/unit-types/route. Do NOT introduce enemy turns or enemy AI. It MUST be purely additive: when no effect is ever applied, behavior is byte-for-byte unchanged, and output stays exactly status/turn/hero_hp/hero_pos/enemies (each enemy exactly {id,hp,pos}).
- 리뷰어 BLOCKING 원본 8 → distinct 7(중복 제거)
- 흡수: decisions 12 / assumed 2 / deferred 1
- 미해소 BLOCKING(흡수 부족분): 0
- interface_contract 파일 수: 3
- acceptance_tests 수: 5

CONTRACT_STATUS: FROZEN

squad 커널 l7 위 가산 카드(l8) — 골렘 base-델타 자율 설계: 변칙적 불균형 일격: 단위가 `asymmetric_strike`(정수)를 가질 때, 공격자의 x좌표가 대상의 x좌표보다 작은 상태(좌측 공격)에서 공격 시 해당 수치만큼 추가 피해 (관측: 공격자의 `pos.x`와 대상의 `pos.x` 비교 결과에 따라 대상의 `hp` 감소량이 결정됨)

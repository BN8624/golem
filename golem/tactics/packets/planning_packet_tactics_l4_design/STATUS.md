# STATUS

- 아이디어: Extend a deterministic tactical-grid combat kernel (one hero vs enemies on a 2D integer grid with minimum coordinate 0; each scenario via --scenario N gives an initial state object {hero:{hp,atk,pos,mana,anomaly_dmg}, enemies:[{id,hp,atk,pos}]} plus any new fields this card needs, and an ordered action list. Action types: move {dir:[dx,dy]} blocked if destination is off-grid (negative), occupied by a living enemy, or a 'Wall' terrain cell; attack {target:id} is melee requiring orthogonal adjacency (Manhattan dist==1) with simultaneous mutual damage; ranged_attack {target:id} requires Manhattan distance 2..3 and deals one-way damage (hero takes nothing, terrain does not block it). A Mana Shield absorbs incoming melee damage before HP, and an ANOMALY discharges hero.anomaly_dmg to orthogonally-adjacent living enemies when the shield ruptures (mana crosses >0 to <=0 within one melee attack), doubled if the hero stands on a 'Conductive' terrain tile. Static terrain is a map of 'x,y' keys to 'Wall'|'Conductive' in the initial state. Kill all enemies = VICTORY, hero hp<=0 = DEFEAT, both dead = DEFEAT, 0 initial enemies or actions exhausted = FINISHED; fully deterministic, no RNG; the CLI prints status/turn/hero_hp/hero_pos/enemies and each enemy is printed as exactly {id,hp,pos}; this output format is FIXED and must not change). NEW CARD 'units (유닛)': introduce unit variety so combatants are no longer identical (for example enemy archetypes with distinct defensive or offensive traits, or a hero unit-class trait). Design the exact deterministic rules yourself: what distinguishes a unit, how it is represented as STATIC data on the existing hero/enemy entities in each scenario's initial state, and how it interacts with melee, ranged, mana shield, anomaly, and terrain. It MUST be a purely additive layer: when entities carry no new unit traits, all existing behavior is byte-for-byte unchanged, and the printed output stays exactly status/turn/hero_hp/hero_pos/enemies with each enemy printed as exactly {id,hp,pos}.
- 리뷰어 BLOCKING 원본 17 → distinct 16(중복 제거)
- 흡수: decisions 10 / assumed 3 / deferred 3
- 미해소 BLOCKING(흡수 부족분): 0
- interface_contract 파일 수: 2
- acceptance_tests 수: 4

CONTRACT_STATUS: FROZEN

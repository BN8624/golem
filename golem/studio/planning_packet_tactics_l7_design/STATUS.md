# STATUS

- 아이디어: Extend a deterministic, HERO-ONLY tactical-grid combat kernel. HARD INVARIANTS: NO enemy turn, NO enemy AI, NO enemy movement, NO enemy mana — enemies are passive targets; the ONLY actor is the hero; the ONLY actions are hero move/attack/ranged_attack from a fixed list; turn increments by 1 per hero action. Six existing cards: (1) hero.mana Mana Shield absorbs melee damage the hero receives; on rupture (mana >0->=<0 in one melee) ANOMALY hits adjacent enemies (Conductive terrain doubles it); (2) ranged_attack 2..3 Manhattan one-way; (3) terrain Wall (blocks hero move, not ranged) / Conductive; (4) enemy unit_type Hardened(melee -1)/Glass(all incoming x2)/Resonant(hero -1 hp per anomaly hit); (5) route map chaining battles (clear current -> load next, hero hp/mana/atk/corrosion persist, pos resets 0,0; final clear = VICTORY); (6) hero.corrosion {dmg,duration} makes ranged inflict Corrosion DoT that ticks each hero action and decays. Scenario --scenario N: initial {hero, enemies:[{id,hp,atk,pos,unit_type?}], terrain?, route?} + action list; deterministic, no RNG; CLI prints status/turn/hero_hp/hero_pos/enemies (each {id,hp,pos}); output FIXED. NEW CARD 'balance config (밸런스/난이도)': an OPTIONAL deterministic difficulty/tuning layer so the same battles can be made easier or harder by data, with measurable effect on outcomes. Design the exact rules yourself: what tunable knobs exist (for example multipliers on the damage the hero deals and/or the damage the hero receives, or on anomaly/corrosion magnitude), how they are represented as an optional config object in the initial state (carried across a route), and EXACTLY where in the hero's action/damage resolution each multiplier applies and how it rounds (integers). Apply everything WITHIN the hero's action resolution (do NOT change how a scenario or battle is loaded, and do NOT add enemy turns). It MUST be purely additive: with no config present, every multiplier is 1 and all existing behavior is byte-for-byte unchanged, and output stays exactly status/turn/hero_hp/hero_pos/enemies (each enemy {id,hp,pos}).
- 리뷰어 BLOCKING 원본 16 → distinct 12(중복 제거)
- 흡수: decisions 15 / assumed 3 / deferred 2
- 미해소 BLOCKING(흡수 부족분): 0
- interface_contract 파일 수: 3
- acceptance_tests 수: 4

CONTRACT_STATUS: FROZEN

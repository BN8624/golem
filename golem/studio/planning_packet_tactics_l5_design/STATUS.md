# STATUS

- 아이디어: Extend a deterministic tactical-grid combat kernel that already has four stacked cards (Mana Shield + ANOMALY rupture; ranged_attack at Manhattan distance 2..3 one-way; static terrain tiles Wall (blocks move, not ranged) and Conductive (doubles anomaly when hero stands on it at rupture); enemy unit_type traits Hardened (melee -1), Glass (all incoming damage x2), Resonant (hero takes 1 self-damage per anomaly hit)). The kernel: one hero vs enemies on a 2D grid (min coord 0); a scenario via --scenario N gives an initial state {hero:{hp,atk,pos,mana,anomaly_dmg}, enemies:[{id,hp,atk,pos,unit_type?}], terrain?} plus an ordered action list of move/attack/ranged_attack; kill all enemies = VICTORY, hero hp<=0 = DEFEAT, both = DEFEAT, 0 initial enemies or actions exhausted = FINISHED; fully deterministic, no RNG; the CLI prints status/turn/hero_hp/hero_pos/enemies (each enemy exactly {id,hp,pos}) and this output format is FIXED and must not change. NEW CARD 'route map (루트 맵)': a campaign layer that links several battles into one route, so a single run can play through a sequence of battles rather than one. A scenario may carry static route data in its initial state describing an ordered list of battles (each battle has its own enemies and optional terrain on the grid). The hero fights the current battle with the shared action list; design the exact deterministic rules yourself: how the route is represented as static data in the initial state, when and how the run advances from one battle to the next, what hero state carries over between battles, how victory/defeat/finished are decided across the whole route, and how every prior mechanic (melee, ranged, mana shield, anomaly, terrain, unit types) behaves within a battle. It MUST be a purely additive layer: a scenario that defines NO route behaves byte-for-byte exactly like a single battle as today, and the printed output stays exactly status/turn/hero_hp/hero_pos/enemies (each enemy printed as exactly {id,hp,pos}).
- 리뷰어 BLOCKING 원본 11 → distinct 11(중복 제거)
- 흡수: decisions 13 / assumed 3 / deferred 2
- 미해소 BLOCKING(흡수 부족분): 0
- interface_contract 파일 수: 3
- acceptance_tests 수: 3

CONTRACT_STATUS: FROZEN
